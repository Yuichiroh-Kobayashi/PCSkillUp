# AGENTS.md

このリポジトリで作業するAIエージェント向け方針です。

## 教材方針

- 中学校技術科のPC操作初学者向け教材であることを前提にしてください。
- 授業品質（安全・迷いにくさ・復帰しやすさ）を最優先にしてください。
- 授業中に迷子を出さないことを優先してください。
- 操作手順は短く明確にしてください。
- 生徒向けUIは日本語で統一してください。
- 個人情報の収集・保存・送信を追加しないでください。
- 解析タグ、トラッカー、外部APIなどの外部サービスを追加しないでください。
- できるだけ外部サービス依存を増やさないでください。
- 教室のネットワーク負荷を増やさないでください。
- GitHub Pagesで動く静的構成（HTML/CSS/JavaScript）を維持してください。

## 実装制約

- 外部ライブラリを追加しないでください。
- 外部画像素材を追加しないでください。
- CDNを使わないでください。
- Google Fonts等の外部フォントを使わないでください。
- Service Worker / PWA化しないでください。
- 明確な理由がない限り、フレームワークやビルドツールを追加しないでください。
- Webアプリ側で Win + Shift + S を検出しないでください。
- Webアプリ側でロイロノート等への貼り付け完了を検出しないでください。
- ステージ2に完全一致クリア判定を追加しないでください。
- BMP保存処理は削除しないでください。
- ただしBMP保存ボタンは現在のUIでは非表示を維持してください。
- 上級モード/ステージ2のBMPは、Windows Paint互換性のため、原則として4bppインデックスBMPと16エントリのパレットを維持してください。変更する場合は理由を明記してください。

## キーボード表示ルール

- キー操作は `<kbd>` を使ったキーキャップ表示を基本としてください。
- Shiftキーは `⇧ Shift` と表示してください。
- Shiftキーは矢印だけにせず、生徒が読めるように `Shift` の文字を必ず残してください。
- Winキーは `Win` と文字で表示してください。
- Windowsロゴ、Microsoftロゴ、Windowsロゴ風の図形は使わないでください。
- AdobeStock等のライセンス不明画像は使わないでください。
- 実物キーボード写真はWebアプリ本体へ追加しないでください。
- 現物風表示を作る場合は、HTML/CSSまたは自作SVGで作ってください。

## ステージ仕様

### ステージ1：ビットゲート

- 16×16
- 1の場所を黒くぬる
- Ctrl + C / Ctrl + V のコピー＆ペースト練習を行う
- 完全クリアでCLEARオーバーレイを表示する
- CLEARオーバーレイには「再チャレンジ」「ステージ2へ」を表示する
- 再チャレンジ時は直前と違う見本を出す

### ステージ2：ピクセルキャプチャー

- 32×32
- 白色・銀色・灰色・黒色の4色
- 完全一致判定なし
- Win + ⇧ Shift + S によるスクリーンショット練習を行う
- 先生が指示するアプリに Ctrl + V でペーストする練習を行う
- PrtScrキーは使わない

## 維持する方針

- 任意テキスト・絵文字の変換機能は将来拡張とし、明示要望がない限りMVPに含めないでください。
- 明示要望がない限り、自動コピー機能ボタンを復活させないでください。
- 明示要望がない限り、0/1データの可視テキストエリアを復活させないでください。
- 手動の Ctrl + C / Ctrl + V 練習はコア要件として扱ってください。
- 明示要望がない限り、Undo / 「1つ戻す」を復活させないでください。授業試行で、繰り返しクリックが多くUndoは有効に働かなかったためです。

## テスト

変更時は最低限以下を実行してください。

```powershell
git diff --check
node --check docs/app.js
node --check docs/patterns.js
node --check docs/bmp.js
```

HTMLコメントや不要なaria-label確認も必要に応じて行ってください。

```powershell
rg -n '<!--' docs/index.html
rg -n 'class="shortcut-guide"[^>]*aria-label' docs/index.html
```
